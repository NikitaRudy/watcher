/**
 * @typedef {string} user e.g root
 * @typedef {number|string} port e.g 22, default 22
 * @typedef {string} host box ip
 * @typedef {string} stb_base_path path to lgioui dir
 * @typedef {string} dir_base_path path to working dir (/src)
 */
const configExample = {
    user: '',
    port: '',
    host: '',
    stbMountPath: '',
    stbBasePath: '',
    dirBasePath: '',
};

module.exports = configExample;
